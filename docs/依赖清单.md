# 依赖清单（模型 + 自定义节点）

> 本仓库**不分发**下面任何文件，安装脚本只负责从**官方源**下载。
> 每个条目的许可请在使用/分发前自行确认。

## 一、ComfyUI 本体

| 项目 | 说明 |
|---|---|
| 版本 | ComfyUI portable v0.36.0（Windows） |
| 大小 | 约 1.83 GB（7z） |
| 来源 | GitHub Releases `Comfy-Org/ComfyUI`，国内走 `gh-proxy.com` / `hk.gh-proxy.com` 镜像 |
| 解压工具 | `7zr.exe`（来自 `ip7z/7zip` Releases，约 0.6 MB） |

## 二、模型（放进 `ComfyUI/models/`）

| 文件 | 目标目录 | 大小 | 来源 | 许可 |
|---|---|---|---|---|
| `flux-2-klein-9b-fp8.safetensors` | `unet/` | 9,433,061,528 B | ModelScope `black-forest-labs/FLUX.2-klein-9b-fp8` | **非商用** |
| `qwen_3_8b_fp8mixed.safetensors` | `clip/` | 8,664,848,742 B | ModelScope `Comfy-Org/flux2-klein-9B`（`split_files/text_encoders/`） | 随底模 |
| `flux2-vae.safetensors` | `vae/` | 336,211,292 B | 同上（`split_files/vae/`） | 随底模 |
| `4x-UltraSharp.pth` | `upscale_models/` | 66,961,958 B | hf-mirror `Kim2091/UltraSharp` | 见上游 |
| `F2K9B_ObjectRemover.safetensors` | `loras/` | 87,070,328 B | hf-mirror `prithivMLmods/FLUX.2-Klein-Object-Remover-Bbox`（288 张量） | 随底模 |
| `General.safetensors` | `BiRefNet/` | 444,473,596 B | hf-mirror `ZhengPeng7/BiRefNet` | MIT（BiRefNet） |

**合计约 18.2 GB。**

- 物体移除 LoRA 的触发词：`Remove the red highlighted object from the scene.`
- 显存基线：底模 9.0 GB + 文本编码器 8.3 GB + VAE 0.32 GB ≈ 17.3 GB
  （所以 16 GB 卡需要来回搬模型：能跑，但慢；基础功能实测峰值 11–13 GB @2.9MP）

## 三、自定义节点（放进 `ComfyUI/custom_nodes/`，6 个一个都不能少）

| 节点包 | 提供的关键节点 | 缺了会怎样 |
|---|---|---|
| `comfyui-KJNodes` | `ImageResizeKJv2`、`ColorMatch`、`PrimitiveStringMultiline` | 还原尺寸 / 颜色匹配失效 |
| `comfyui_essentials` | **`GetImageSize+`** | 提交时报 `missing_node_type: GetImageSize+` |
| `RES4LYF` | **`beta57` 采样器** | 报 `scheduler 'beta57' not in [...]`（插件会自动退回 `beta`） |
| `ComfyUI_BiRefNet_ll` | `AutoDownloadBiRefNetModel`、`GetMaskByBiRefNet` | 「自动保护人物」不可用 |
| `Comfyui-PainterFluxImageEdit` | `PainterFluxImageEdit` | 无法提交工作流 |
| `cleanbg_control`（本仓库自带） | `GET /cleanbg/ping`、`POST /cleanbg/shutdown` | 面板【关闭 ComfyUI】退化成跑脚本 |

## 四、工作流结构（供排查用）

```
19 LoadImage
90 ImageScaleToTotalPixels
91 GetImageSize+
21/22/23  CheckpointLoader(unet) / CLIPLoader(flux2) / VAELoader
24 PrimitiveStringMultiline（提示词）
25 PainterFluxImageEdit（image1_mask ← 蒙版）
26 KSampler（euler / beta57 / cfg 1.0 / 4 steps）
27 VAEDecode
30/31 4x-UltraSharp 放大
102 GetImageSize+
32/34 ImageResizeKJv2（还原到原像素尺寸）
33 ColorMatch（mkl，强度 1.0）
35 ImageCompositeMasked（用同一蒙版合成）
41 SaveImage

protect 模式额外：
60 AutoDownloadBiRefNetModel(General) → 61 GetMaskByBiRefNet → 62 GrowMask(+8)
→ 63 InvertMask → 64/65/66 蒙版尺寸对齐
```

蒙版语义：`image1_mask` 同时写进 latent 的 `noise_mask`（1 = 重绘）并用同一蒙版做合成，
所以**蒙版外 0 改动**（实测人物区最大偏差 1–2/255，>3 的像素占比 0.0000%）。

## 五、法务备注

- **FLUX.2 klein 是非商用许可** → 本项目不能用于商业用途；商用需向 Black Forest Labs 取得授权。
- ComfyUI 与各节点多为 GPL-3.0：**再分发**须附许可证并提供源码。
  本仓库采用"仅脚本 + 官方源下载"的方式规避该义务，请勿改成内置分发。
- 不要在本仓库里加入任何来源不明的加密模型或私有资产。

---

## 六、自建镜像（防止第三方链接失效）

原作者把模型转存到了自己的魔搭仓库，安装脚本**优先**从这里下载，官方源 / `hf-mirror.com` 只作为兜底。
仓库地址与链接形态：

| 项目 | 值 |
|---|---|
| 魔搭仓库 | `zdccy123/cleanbg-models`（公开） |
| 固定版本 | 标签 **`v2`** —— 脚本里写 `Revision=v2`，**不随 master 变动**（换版本必须显式改脚本） |
| 链接模板 | `https://modelscope.cn/api/v1/models/zdccy123/cleanbg-models/repo?Revision=v2&FilePath=<URL编码的仓库内路径>` |

| 仓库内路径 | 对应的本地目标 | 大小 |
|---|---|---|
| `vae/flux2-vae.safetensors` | `models/vae/` | 336,211,292 B |
| `clip/qwen_3_8b_fp8mixed.safetensors` | `models/clip/` | 8,664,848,742 B |
| `upscale_models/4x-UltraSharp.pth` | `models/upscale_models/` | 66,961,958 B |
| `loras/F2K9B_ObjectRemover.safetensors` | `models/loras/` | 87,070,328 B |
| `BiRefNet/General.safetensors` | `models/BiRefNet/` | 444,473,596 B |

**主模型 `unet/flux-2-klein-9b-fp8.safetensors`（9,433,061,528 B）仍走魔搭官方组织仓库**
`black-forest-labs/FLUX.2-klein-9b-fp8`（官方组织，链接稳定）。
脚本里也预留了自建镜像地址在它前面 —— 以后若把它也转存进本仓库，脚本会自动优先用自建镜像（找不到就 404，自动回退官方源）。

### 六个模型的 SHA256（安装后可自行核对）

```
865ba09f5b4c3cbd3468…  unet/flux-2-klein-9b-fp8.safetensors      （完整值见安装脚本里的 $shaMap）
be2e86b1dd68bcb6818a…  clip/qwen_3_8b_fp8mixed.safetensors
868fe7b343cc8f3a19db…  vae/flux2-vae.safetensors
a5812231fc936b42af08…  upscale_models/4x-UltraSharp.pth
d3b728e744b09ddf5ea2…  loras/F2K9B_ObjectRemover.safetensors
9ab37426bf4de0567af6…  BiRefNet/General.safetensors
```

> **完整 64 位哈希**写在 `installer/auto/install.ps1` 的 `$shaMap` 里；安装时会对 4 个小模型自动做
> SHA256 校验（不匹配就删掉并提示重下），两个大模型只校验文件大小以节省时间。
>
> 小知识：魔搭按**文件内容（blob 哈希）去重**。转存已经在魔搭上存在过的文件是"秒传"——
> 只登记哈希、不经网络传输，几乎不消耗上传者的流量。
